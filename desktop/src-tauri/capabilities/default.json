{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main library window. Reader windows load remote scanlation sites and get no IPC at all — progress tracking happens on the Rust side via page-load hooks.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "notification:default",
    "opener:default"
  ]
}
